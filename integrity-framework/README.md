# Integrity Framework

This directory contains foundational framework components, including early iterations of the Integrity Protocol's smart contracts and supporting Rust services.

> **Note:** This package is currently deprecated/archived. Active development on contracts and backend services happens in the `contracts/` and `integrity-oracle/` directories respectively.
