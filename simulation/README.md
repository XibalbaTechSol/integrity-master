# Simulation

This directory contains test simulations and autoresearch frameworks for the Integrity Protocol. It is used to stress-test the `integrity-oracle` and validate full-stack BAA execution flows.

## Usage

```bash
# Ensure you have the required dependencies (or use the oracle's environment)
python stress_test_simulation.py
```
